# Credenciais — nunca versionar
.env
.env.local
credentials.json
token.json
*.pem

# E-mails reais da caixa de teste (contêm dados de terceiros)
fixtures/*.eml
fixtures/attachments/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node / Next.js
node_modules/
.next/
out/
.turbo/

# Sistema
.DS_Store
*.log

# Anexos e cache de LLM. O .storage guarda PDFs de terceiros (boletos reais de
# fornecedores do cliente) — nunca vão para o repositório. O conteúdo canônico dos
# documentos fica no Postgres; o disco local é só conveniência de desenvolvimento.
.storage/
**/.storage/
.cache/

# Anexos e e-mails reais ficam fora; os RÓTULOS do golden set são versionados
!apps/api/tests/golden/casos.json

# Vercel
.vercel
