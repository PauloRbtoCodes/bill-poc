# Dependências da função serverless. Deliberadamente separadas do pyproject de
# desenvolvimento: o limite de tamanho da função é 250MB descompactados, e ruff, pytest
# e afins não têm o que fazer em produção.
fastapi>=0.115
pydantic>=2.9
psycopg[binary]>=3.2
python-dotenv>=1.0
anthropic>=0.69
pdfplumber>=0.11
# Nenhuma biblioteca do Google aqui. A API do Gmail é chamada por HTTP direto e o
# refresh do token é um POST no endpoint OAuth2 padrão — ambos com urllib, em
# ingest/gmail.py. O google-api-python-client embutiria 101MB de documentos de descoberta
# de 598 APIs para usar dois endpoints, e o google-auth arrastaria a `requests` junto.
# O fluxo de autorização inicial (google-auth-oauthlib) roda só na máquina do
# desenvolvedor, via `billpoc auth`, e por isso fica fora daqui.
